pipeline {
   agent any

   stages {
      stage('Install node modules') {
         steps {
            sh 'npm install'
         }
      }
      stage('Bye') {
         steps {
            echo 'Goodbye!'
         }
      }
   }
}
